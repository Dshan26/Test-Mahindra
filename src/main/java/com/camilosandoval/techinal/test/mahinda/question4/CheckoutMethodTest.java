package com.camilosandoval.techinal.test.mahinda.question4;

public class CheckoutMethodTest {

  //TODO checkout Explication in file REAdME.md
  /*public boolean checkCredendials() {
    UserCredentials credentials = get_login_form_input();

    User user = fetchUserProfileFromDB(credentials.username);
    if (user == null)
      throw new LoginFailedException(“invalid username”);

    if (credendials.password != user.password) {
      throw new LoginFailedException(“Invalid Password”);
    }

    LOGGER.info(“User {} logged in using password {}”,
    credentials.username, credentials.password);

    return true;
  }
*/


}
